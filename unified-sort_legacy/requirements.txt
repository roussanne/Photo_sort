# Core
streamlit>=1.25
opencv-python>=4.8
numpy>=1.24
pandas>=2.0
pillow>=9.5

# Optional (uncomment if you want the extra features)
# pillow-heif>=0.13      # HEIC/HEIF support
# rawpy>=0.18            # High-quality RAW processing (RW2, CR2, NEF...)
# imageio>=2.31          # imageio.v3 API (write/read), used as RAW fallback & JPG save
# send2trash>=1.8        # Safe delete to OS trash

# If your unified_sort library uses plotly etc., add here:
# plotly>=5.18

# 선택 의존성은 주석 처리